apiVersion: v1
kind: Namespace
metadata:
  name: quota-test
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: sample-appliedquota
  namespace: quota-test
spec:
  hard:
    limits.cpu: "500m"
    limits.memory: "512Mi"
    # requests.cpu: "1500m"
    # requests.memory: "512Mi"