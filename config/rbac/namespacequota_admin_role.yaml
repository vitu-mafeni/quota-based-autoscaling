# This rule is not used by the project quota-based-autoscaling itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full permissions ('*') over scaling.dcn.ssu.ac.kr.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: quota-based-autoscaling
    app.kubernetes.io/managed-by: kustomize
  name: namespacequota-admin-role
rules:
- apiGroups:
  - scaling.dcn.ssu.ac.kr
  resources:
  - namespacequotas
  verbs:
  - '*'
- apiGroups:
  - scaling.dcn.ssu.ac.kr
  resources:
  - namespacequotas/status
  verbs:
  - get
